<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheets" href="/public/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
<div class="container mx-auto p-10">
        <div class="nav mb-10">
        <a href="/read" class="text-blue-500 hover:underline">Update Users</a>
    </div>
    <h1 class="text-2xl font-bold mb-4">Update User</h1>
<form action="/update/<%= user._id %>" method="POST" class="flex flex-col gap-4 w-1/3">
    <input type="name" class="border border-gray-300 p-2 rounded" placeholder="Enter your name" name="name" value="<%= user.name %>" >
    <input type="email" class="border border-gray-300 p-2 rounded" placeholder="Enter your email" name="email" value="<%= user.email %>" required >
    <input  class="border border-gray-300 p-2 rounded" placeholder="Image url" name="image" value="<%= user.image %>" >
    <button type="submit" class="bg-yellow-500 text-black p-2 rounded hover:bg-blue-600" value="Update User">Update</button>
</form>

</div>
</body>
</html>  