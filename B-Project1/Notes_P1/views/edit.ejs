<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Task</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class=" bg-gradient-to-br from-gray-900 via-zinc-900 to-black flex items-center justify-center min-h-screen font-sans">
    <a href="/" class="absolute top-4 left-4 text-blue-500 hover:underline">Back to Task List</a>
    <form action="/edit/<%= title %>" method="post" class="bg-zinc-800 p-8 rounded-2xl shadow-lg w-full space-y-4">
        <h2 class="text-2xl font-bold text-white mb-6 text-center">Edit Task</h2>
        <div>
            <label class="block text-gray-300 text-sm mb-2">Task Title</label>
            <input 
                type="text" 
                name="title"
                value="<%= title %>"
                placeholder=" Previous task title"
                class="w-full px-4 py-2 rounded-lg bg-zinc-900 text-white border border-zinc-700 focus:ring-2 focus:ring-blue-500 focus:outline-none"
            >
<input type="hidden" name="oldTitle" value="<%= title %>">
        </div>
        <div>
            <label class="block text-gray-300 text-sm mb-2">Description</label>
            <textarea 
                name="description"
                rows="4"
                class="w-full px-4 py-2 rounded-lg bg-zinc-900 text-white border border-zinc-700 resize-none focus:ring-2 focus:ring-blue-500 focus:outline-none"
            ><%= description %></textarea>
        </div>
        <div class="flex justify-center">
            <button 
                type="submit" 
                class="w-full py-2 px-4 rounded-lg bg-blue-600 hover:bg-blue-700 text-white font-semibold shadow-md transition duration-200"
            >
                + Update Task
            </button>
        </div>
    </form>
</body>
</html>
