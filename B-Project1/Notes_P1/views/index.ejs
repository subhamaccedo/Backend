<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Task</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class=" bg-gradient-to-br from-gray-900 via-zinc-900 to-black flex items-center justify-center min-h-screen font-sans">
  <div class="bg-zinc-800 p-8 rounded-2xl shadow-lg w-full">
    <div> 
        <h2 class="text-2xl font-bold text-white mb-6 text-center">Create a New Task</h2>
    <form action="/create" method="post"class="space-y-4">
      <div>
        <label class="block text-gray-300 text-sm mb-2">Task Title</label>
        <input 
          type="text" 
          name="title"
          placeholder="Enter task title" 
          class="w-full px-4 py-2 rounded-lg bg-zinc-900 text-white border border-zinc-700 focus:ring-2 focus:ring-blue-500 focus:outline-none"
        >
      </div>
            <div>
        <label class="block text-gray-300 text-sm mb-2">Description</label>
        <textarea 
          placeholder="Enter task details..." 
          name="description"
          rows="4"
          class="w-full px-4 py-2 rounded-lg bg-zinc-900 text-white border border-zinc-700 resize-none focus:ring-2 focus:ring-blue-500 focus:outline-none"
        ></textarea>
      </div>
            <div class="flex justify-center">
        <button 
          type="submit" 
          class="w-full py-2 px-4 rounded-lg bg-blue-600 hover:bg-blue-700 text-white font-semibold shadow-md transition duration-200"
        >
          + Create Task
        </button>
      </div>
    </form>
    </div>
    <div class="tasks flex gap-3 flex-wrap p-10">
      <% if (files && files.length > 0) { %>
        <% files.forEach(function(val) { %>
          <div class="bg-zinc-900 p-4 rounded-lg shadow-md w-1/4">
            <h3 class="text-white text-lg font-semibold mb-2"><%= val %></h3>
            <a class="text-blue-500 hover:underline" href="/files/<%= val %>">View Task</a>
            <a class="text-red-500 p-4 hover:underline" href="/delete/<%= val %>">Delete Task</a>
            <a class="text-green-500 hover:underline" href="/edit/<%= val %>">Edit Task</a>
          </div>
        <% }) %>
      <% } else { %>
        <div class="text-white text-sm">No tasks available.</div>
      <% } %>  
  </div>
</body>
</html>
