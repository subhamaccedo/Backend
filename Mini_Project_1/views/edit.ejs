<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Edit Post</title>
  </head>
  <body class="bg-gradient-to-r from-yellow-50 to-orange-50 min-h-screen flex items-center justify-center">
    <div class="w-full max-w-2xl bg-white rounded-xl shadow-lg p-8">

      <!-- Form -->
      <form
        action="/update/<%= post._id %>"
        method="post"
        class="space-y-4"
      >
        <label for="content" class="block text-gray-600 font-medium"
          >Update your post:</label
        >
        <textarea
          id="content"
          rows="5"
          class="w-full resize-none bg-gray-50 border border-gray-300 p-3 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-yellow-400"
          name="content"
          required
        ><%= post.content %></textarea>

        <button
          type="submit"
          class="w-full bg-yellow-500 hover:bg-yellow-600 text-black font-semibold px-6 py-3 rounded-lg shadow-md transition"
        >
           Update Post
        </button>
      </form>
    </div>
  </body>
</html>
